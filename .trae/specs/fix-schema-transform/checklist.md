- [x] 对每个 sheet 能在前 N 行内识别表头行；异常表可回退且不中断处理
- [x] 输出第1行=表头字段，第2行=数据类型，第3行=注释/中文表头；结构稳定
- [x] 缺失类型行时能插入并基于真实数据区域推断填充类型
- [x] 注释行插入位置正确，且每列注释与表头字段一一对应并已翻译
- [x] 能跳过注释行后的空行/无效行，识别首个连续非空数据块用于类型推断
- [x] A列左侧新增列写入 A1=##var、A2=##type、A3=##，并保持 A4~AN 为空
- [x] 后续搜索/导出逻辑仍按约定读取且回归测试通过
